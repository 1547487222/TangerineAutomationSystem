<UserControl x:Class="TangerineAutomationSystem.Views.ModuleFunctionCatalogEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="模块功能表" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="添加功能" Click="AddFunction_Click" Margin="0,0,8,0" Padding="8,4" />
            <Button Content="删除功能" Click="DeleteFunction_Click" Padding="8,4" />
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="FunctionGrid" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="功能名称" Binding="{Binding Name}" Width="150" />
                <DataGridTextColumn Header="显示名称" Binding="{Binding DisplayName}" Width="150" />
                <DataGridTextColumn Header="模块类型" Binding="{Binding ModuleType}" Width="120" />
                <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
